[build]
builder = "NIXPACKS"
buildCommand = "npm install -g pnpm && pnpm install && pnpm run build"

[deploy]
startCommand = "pnpm run setup && pnpm run start"
healthcheckPath = "/"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
